
  # MTA Bus Route Tracker

  This is a code bundle for MTA Bus Route Tracker. The original project is available at https://www.figma.com/design/8DJ5KBAk11dMZRM5Eh3V7P/MTA-Bus-Route-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  